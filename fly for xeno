-- ================================
-- DRAGON FLY SCRIPT PRO V2 FIXED
-- Script di Volo Avanzato per Xeno e Delta
-- ================================

local player = game.Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local character = player.Character or player.CharacterAdded:Wait()
local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
local humanoid = character:WaitForChild("Humanoid")

-- ================================
-- CONFIGURAZIONE
-- ================================
local Config = {
    flying = false,
    speed = 50,
    maxSpeed = 200,
    acceleration = 5,
    panelOpen = true,
    currentMode = "Normal",
    modes = {"Normal", "Smooth", "Turbo"}
}

-- Variabili
local bodyVelocity
local bodyGyro
local connection
local screenGui
local mainFrame
local speedLabel
local statusLabel
local closeButton
local minimizeIcon
local sliderActive = false

-- ================================
-- FUNZIONI UTILITY
-- ================================
local function clamp(value, min, max)
    return math.max(min, math.min(max, value))
end

local function round(num, numDecimalPlaces)
    local mult = 10^(numDecimalPlaces or 0)
    return math.floor(num * mult + 0.5) / mult
end

-- ================================
-- CREAZIONE GUI AVANZATA
-- ================================
local function createGui()
    if screenGui then screenGui:Destroy() end
    
    screenGui = Instance.new("ScreenGui")
    screenGui.Name = "FlyGuiProV2Fixed"
    screenGui.ResetOnSpawn = false
    screenGui.DisplayOrder = 999
    screenGui.Parent = playerGui
    
    -- ===== FRAME PRINCIPALE =====
    mainFrame = Instance.new("Frame")
    mainFrame.Name = "MainFrame"
    mainFrame.Size = UDim2.new(0, 420, 0, 550)
    mainFrame.Position = UDim2.new(0.5, -210, 0.5, -275)
    mainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 20)
    mainFrame.BorderSizePixel = 0
    mainFrame.Parent = screenGui
    
    -- Effetto glassmorphism
    local mainCorner = Instance.new("UICorner")
    mainCorner.CornerRadius = UDim.new(0, 25)
    mainCorner.Parent = mainFrame
    
    local mainStroke = Instance.new("UIStroke")
    mainStroke.Color = Color3.fromRGB(255, 120, 0)
    mainStroke.Thickness = 4
    mainStroke.LineJoinMode = Enum.LineJoinMode.Round
    mainStroke.Parent = mainFrame
    
    -- Sfondo con gradient
    local bgGradient = Instance.new("UIGradient")
    bgGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(20, 10, 30)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(10, 10, 20))
    })
    bgGradient.Rotation = 45
    bgGradient.Parent = mainFrame
    
    -- ===== HEADER PREMIUM =====
    local header = Instance.new("Frame")
    header.Size = UDim2.new(1, 0, 0, 120)
    header.Position = UDim2.new(0, 0, 0, 0)
    header.BackgroundColor3 = Color3.fromRGB(30, 15, 40)
    header.BorderSizePixel = 0
    header.Parent = mainFrame
    
    local headerCorner = Instance.new("UICorner")
    headerCorner.CornerRadius = UDim.new(0, 25)
    headerCorner.Parent = header
    
    local headerGradient = Instance.new("UIGradient")
    headerGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 100, 0)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(30, 15, 40))
    })
    headerGradient.Rotation = 90
    headerGradient.Parent = header
    
    -- Logo animato
    local logoLabel = Instance.new("TextLabel")
    logoLabel.Name = "Logo"
    logoLabel.Size = UDim2.new(0, 80, 0, 80)
    logoLabel.Position = UDim2.new(0, 20, 0, 15)
    logoLabel.BackgroundTransparency = 1
    logoLabel.Font = Enum.Font.GothamBold
    logoLabel.TextSize = 70
    logoLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    logoLabel.TextScaled = true
    logoLabel.Parent = header
    logoLabel.Text = "🐉"
    
    -- Titolo premium
    local titleLabel = Instance.new("TextLabel")
    titleLabel.Name = "Title"
    titleLabel.Size = UDim2.new(0, 250, 0, 50)
    titleLabel.Position = UDim2.new(0, 110, 0, 10)
    titleLabel.BackgroundTransparency = 1
    titleLabel.Font = Enum.Font.GothamBold
    titleLabel.TextSize = 32
    titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    titleLabel.TextXAlignment = Enum.TextXAlignment.Left
    titleLabel.TextScaled = true
    titleLabel.Parent = header
    titleLabel.Text = "DRAGON FLY"
    
    -- Sottotitolo
    local subtitleLabel = Instance.new("TextLabel")
    subtitleLabel.Name = "Subtitle"
    subtitleLabel.Size = UDim2.new(0, 250, 0, 30)
    subtitleLabel.Position = UDim2.new(0, 110, 0, 50)
    subtitleLabel.BackgroundTransparency = 1
    subtitleLabel.Font = Enum.Font.Gotham
    subtitleLabel.TextSize = 14
    subtitleLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
    subtitleLabel.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLabel.Parent = header
    subtitleLabel.Text = "✨ PRO V2 - Ultimate Flight Script"
    
    -- Status live
    statusLabel = Instance.new("TextLabel")
    statusLabel.Name = "Status"
    statusLabel.Size = UDim2.new(0, 250, 0, 25)
    statusLabel.Position = UDim2.new(0, 110, 0, 80)
    statusLabel.BackgroundTransparency = 1
    statusLabel.Font = Enum.Font.Gotham
    statusLabel.TextSize = 12
    statusLabel.TextColor3 = Color3.fromRGB(100, 255, 100)
    statusLabel.TextXAlignment = Enum.TextXAlignment.Left
    statusLabel.Parent = header
    statusLabel.Text = "● PRONTO"
    
    -- ===== PULSANTE CHIUDI CON ICONA =====
    closeButton = Instance.new("TextButton")
    closeButton.Name = "CloseButton"
    closeButton.Size = UDim2.new(0, 50, 0, 50)
    closeButton.Position = UDim2.new(1, -70, 0, 10)
    closeButton.BackgroundColor3 = Color3.fromRGB(255, 60, 60)
    closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    closeButton.Font = Enum.Font.GothamBold
    closeButton.TextSize = 28
    closeButton.Parent = mainFrame
    closeButton.Text = "✕"
    closeButton.BorderSizePixel = 0
    closeButton.ZIndex = 100
    
    local closeCorner = Instance.new("UICorner")
    closeCorner.CornerRadius = UDim.new(0, 15)
    closeCorner.Parent = closeButton
    
    local closeStroke = Instance.new("UIStroke")
    closeStroke.Color = Color3.fromRGB(255, 100, 100)
    closeStroke.Thickness = 2
    closeStroke.Parent = closeButton
    
    -- ===== ICONA MINIMIZE =====
    minimizeIcon = Instance.new("TextLabel")
    minimizeIcon.Name = "MinimizeIcon"
    minimizeIcon.Size = UDim2.new(0, 50, 0, 50)
    minimizeIcon.Position = UDim2.new(1, -130, 0, 10)
    minimizeIcon.BackgroundColor3 = Color3.fromRGB(100, 100, 150)
    minimizeIcon.TextColor3 = Color3.fromRGB(255, 255, 255)
    minimizeIcon.Font = Enum.Font.GothamBold
    minimizeIcon.TextSize = 28
    minimizeIcon.Parent = mainFrame
    minimizeIcon.Text = "−"
    minimizeIcon.BorderSizePixel = 0
    minimizeIcon.Visible = false
    minimizeIcon.ZIndex = 100
    
    local minimizeCorner = Instance.new("UICorner")
    minimizeCorner.CornerRadius = UDim.new(0, 15)
    minimizeCorner.Parent = minimizeIcon
    
    local minimizeStroke = Instance.new("UIStroke")
    minimizeStroke.Color = Color3.fromRGB(120, 120, 180)
    minimizeStroke.Thickness = 2
    minimizeStroke.Parent = minimizeIcon
    
    closeButton.MouseButton1Click:Connect(function()
        Config.panelOpen = false
        mainFrame.Visible = false
        minimizeIcon.Visible = true
    end)
    
    minimizeIcon.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            Config.panelOpen = true
            mainFrame.Visible = true
            minimizeIcon.Visible = false
        end
    end)
    
    -- ===== SEZIONE VELOCITÀ PREMIUM =====
    local speedSection = Instance.new("Frame")
    speedSection.Size = UDim2.new(0.9, 0, 0, 90)
    speedSection.Position = UDim2.new(0.05, 0, 0, 135)
    speedSection.BackgroundColor3 = Color3.fromRGB(25, 15, 35)
    speedSection.BorderSizePixel = 0
    speedSection.Parent = mainFrame
    
    local speedCorner = Instance.new("UICorner")
    speedCorner.CornerRadius = UDim.new(0, 15)
    speedCorner.Parent = speedSection
    
    local speedStroke = Instance.new("UIStroke")
    speedStroke.Color = Color3.fromRGB(255, 100, 0)
    speedStroke.Thickness = 2
    speedStroke.Parent = speedSection
    
    -- Title velocità
    local speedTitleLabel = Instance.new("TextLabel")
    speedTitleLabel.Name = "SpeedTitle"
    speedTitleLabel.Size = UDim2.new(1, 0, 0, 25)
    speedTitleLabel.Position = UDim2.new(0, 15, 0, 8)
    speedTitleLabel.BackgroundTransparency = 1
    speedTitleLabel.Font = Enum.Font.GothamBold
    speedTitleLabel.TextSize = 14
    speedTitleLabel.TextColor3 = Color3.fromRGB(255, 150, 0)
    speedTitleLabel.TextXAlignment = Enum.TextXAlignment.Left
    speedTitleLabel.Parent = speedSection
    speedTitleLabel.Text = "⚡ VELOCITÀ"
    
    -- Display velocità
    speedLabel = Instance.new("TextLabel")
    speedLabel.Name = "SpeedDisplay"
    speedLabel.Size = UDim2.new(1, -30, 0, 25)
    speedLabel.Position = UDim2.new(0, 15, 0, 35)
    speedLabel.BackgroundTransparency = 1
    speedLabel.Font = Enum.Font.Gotham
    speedLabel.TextSize = 16
    speedLabel.TextColor3 = Color3.fromRGB(100, 255, 150)
    speedLabel.TextXAlignment = Enum.TextXAlignment.Left
    speedLabel.Parent = speedSection
    speedLabel.Text = "50 / 200"
    
    -- Slider
    local sliderBg = Instance.new("Frame")
    sliderBg.Size = UDim2.new(0.9, 0, 0, 12)
    sliderBg.Position = UDim2.new(0.05, 0, 0, 65)
    sliderBg.BackgroundColor3 = Color3.fromRGB(40, 30, 50)
    sliderBg.BorderSizePixel = 0
    sliderBg.Parent = speedSection
    sliderBg.ZIndex = 10
    
    local sliderCorner = Instance.new("UICorner")
    sliderCorner.CornerRadius = UDim.new(0, 6)
    sliderCorner.Parent = sliderBg
    
    local sliderFill = Instance.new("Frame")
    sliderFill.Size = UDim2.new(0.25, 0, 1, 0)
    sliderFill.BackgroundColor3 = Color3.fromRGB(255, 100, 0)
    sliderFill.BorderSizePixel = 0
    sliderFill.Parent = sliderBg
    
    local fillCorner = Instance.new("UICorner")
    fillCorner.CornerRadius = UDim.new(0, 6)
    fillCorner.Parent = sliderFill
    
    local fillGradient = Instance.new("UIGradient")
    fillGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 100, 0)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 150, 0))
    })
    fillGradient.Parent = sliderFill
    
    local updateSlider = function(mousePos)
        local relativePos = clamp((mousePos.X - sliderBg.AbsolutePosition.X) / sliderBg.AbsoluteSize.X, 0, 1)
        Config.speed = clamp(relativePos * Config.maxSpeed, 1, Config.maxSpeed)
        sliderFill.Size = UDim2.new(relativePos, 0, 1, 0)
        speedLabel.Text = round(Config.speed, 1) .. " / " .. Config.maxSpeed
    end
    
    sliderBg.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            sliderActive = true
            updateSlider(game:GetService("UserInputService"):GetMouseLocation())
        end
    end)
    
    game:GetService("UserInputService").InputChanged:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.MouseMovement and sliderActive then
            updateSlider(game:GetService("UserInputService"):GetMouseLocation())
        end
    end)
    
    game:GetService("UserInputService").InputEnded:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            sliderActive = false
        end
    end)
    
    -- ===== SEZIONE MODALITÀ PREMIUM =====
    local modeSection = Instance.new("Frame")
    modeSection.Size = UDim2.new(0.9, 0, 0, 85)
    modeSection.Position = UDim2.new(0.05, 0, 0, 235)
    modeSection.BackgroundColor3 = Color3.fromRGB(25, 15, 35)
    modeSection.BorderSizePixel = 0
    modeSection.Parent = mainFrame
    modeSection.ZIndex = 10
    
    local modeCorner = Instance.new("UICorner")
    modeCorner.CornerRadius = UDim.new(0, 15)
    modeCorner.Parent = modeSection
    
    local modeStroke = Instance.new("UIStroke")
    modeStroke.Color = Color3.fromRGB(255, 100, 0)
    modeStroke.Thickness = 2
    modeStroke.Parent = modeSection
    
    local modeTitle = Instance.new("TextLabel")
    modeTitle.Name = "ModeTitle"
    modeTitle.Size = UDim2.new(1, 0, 0, 25)
    modeTitle.Position = UDim2.new(0, 15, 0, 8)
    modeTitle.BackgroundTransparency = 1
    modeTitle.Font = Enum.Font.GothamBold
    modeTitle.TextSize = 14
    modeTitle.TextColor3 = Color3.fromRGB(255, 150, 0)
    modeTitle.TextXAlignment = Enum.TextXAlignment.Left
    modeTitle.Parent = modeSection
    modeTitle.Text = "🎮 MODALITÀ VOLO"
    
    local modes = {"Normal", "Smooth", "Turbo"}
    for i, mode in ipairs(modes) do
        local modeButton = Instance.new("TextButton")
        modeButton.Name = mode .. "Button"
        modeButton.Size = UDim2.new(0.28, 0, 0, 35)
        modeButton.Position = UDim2.new((i-1) * 0.32 + 0.05, 0, 0, 40)
        modeButton.BackgroundColor3 = i == 1 and Color3.fromRGB(255, 100, 0) or Color3.fromRGB(45, 30, 60)
        modeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
        modeButton.Font = Enum.Font.GothamBold
        modeButton.TextSize = 13
        modeButton.Parent = modeSection
        modeButton.Text = mode
        modeButton.BorderSizePixel = 0
        modeButton.ZIndex = 20
        
        local modeCorner2 = Instance.new("UICorner")
        modeCorner2.CornerRadius = UDim.new(0, 10)
        modeCorner2.Parent = modeButton
        
        local modeStroke2 = Instance.new("UIStroke")
        modeStroke2.Color = i == 1 and Color3.fromRGB(255, 150, 0) or Color3.fromRGB(100, 100, 120)
        modeStroke2.Thickness = 2
        modeStroke2.Parent = modeButton
        
        modeButton.MouseButton1Click:Connect(function()
            Config.currentMode = mode
            for _, btn in ipairs(modeSection:GetChildren()) do
                if btn:IsA("TextButton") then
                    local isSelected = btn.Name == mode .. "Button"
                    btn.BackgroundColor3 = isSelected and Color3.fromRGB(255, 100, 0) or Color3.fromRGB(45, 30, 60)
                    btn:FindFirstChild("UIStroke").Color = isSelected and Color3.fromRGB(255, 150, 0) or Color3.fromRGB(100, 100, 120)
                end
            end
            statusLabel.Text = "● MODALITÀ: " .. mode:upper()
        end)
    end
    
    -- ===== PULSANTE FLY PRINCIPALE - SEPARATO DAL SLIDER =====
    local flyButton = Instance.new("TextButton")
    flyButton.Name = "FlyButton"
    flyButton.Size = UDim2.new(0.9, 0, 0, 55)
    flyButton.Position = UDim2.new(0.05, 0, 0, 335)
    flyButton.BackgroundColor3 = Color3.fromRGB(255, 100, 0)
    flyButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    flyButton.Font = Enum.Font.GothamBold
    flyButton.TextSize = 20
    flyButton.Parent = mainFrame
    flyButton.Text = "🚀 ATTIVA VOLO"
    flyButton.BorderSizePixel = 0
    flyButton.ZIndex = 50
    
    local flyCorner = Instance.new("UICorner")
    flyCorner.CornerRadius = UDim.new(0, 15)
    flyCorner.Parent = flyButton
    
    local flyStroke = Instance.new("UIStroke")
    flyStroke.Color = Color3.fromRGB(255, 150, 0)
    flyStroke.Thickness = 3
    flyStroke.Parent = flyButton
    
    local flyGradient = Instance.new("UIGradient")
    flyGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 120, 0)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 80, 0))
    })
    flyGradient.Parent = flyButton
    
    -- Click event PRINCIPALE del pulsante FLY
    local flyButtonConnected = false
    if not flyButtonConnected then
        flyButtonConnected = true
        flyButton.MouseButton1Click:Connect(function()
            print("Pulsante FLY cliccato!")
            if Config.flying then
                stopFlying()
                flyButton.BackgroundColor3 = Color3.fromRGB(255, 100, 0)
                flyButton.Text = "🚀 ATTIVA VOLO"
                statusLabel.Text = "● PRONTO"
                statusLabel.TextColor3 = Color3.fromRGB(100, 255, 100)
            else
                startFlying()
                flyButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
                flyButton.Text = "⛔ DISATTIVA VOLO"
                statusLabel.Text = "● IN VOLO - " .. Config.currentMode:upper()
                statusLabel.TextColor3 = Color3.fromRGB(255, 100, 0)
            end
        end)
    end
    
    -- Effetto hover sul pulsante
    flyButton.MouseEnter:Connect(function()
        if not Config.flying then
            flyButton.BackgroundColor3 = Color3.fromRGB(255, 130, 30)
        end
    end)
    
    flyButton.MouseLeave:Connect(function()
        if not Config.flying then
            flyButton.BackgroundColor3 = Color3.fromRGB(255, 100, 0)
        end
    end)
    
    -- ===== SEZIONE CONTROLLI =====
    local controlsSection = Instance.new("Frame")
    controlsSection.Size = UDim2.new(0.9, 0, 0, 115)
    controlsSection.Position = UDim2.new(0.05, 0, 0, 405)
    controlsSection.BackgroundColor3 = Color3.fromRGB(25, 15, 35)
    controlsSection.BorderSizePixel = 0
    controlsSection.Parent = mainFrame
    controlsSection.ZIndex = 10
    
    local controlsCorner = Instance.new("UICorner")
    controlsCorner.CornerRadius = UDim.new(0, 15)
    controlsCorner.Parent = controlsSection
    
    local controlsStroke = Instance.new("UIStroke")
    controlsStroke.Color = Color3.fromRGB(100, 200, 255)
    controlsStroke.Thickness = 2
    controlsStroke.Parent = controlsSection
    
    local controlsTitle = Instance.new("TextLabel")
    controlsTitle.Name = "ControlsTitle"
    controlsTitle.Size = UDim2.new(1, 0, 0, 25)
    controlsTitle.Position = UDim2.new(0, 15, 0, 8)
    controlsTitle.BackgroundTransparency = 1
    controlsTitle.Font = Enum.Font.GothamBold
    controlsTitle.TextSize = 14
    controlsTitle.TextColor3 = Color3.fromRGB(100, 200, 255)
    controlsTitle.TextXAlignment = Enum.TextXAlignment.Left
    controlsTitle.Parent = controlsSection
    controlsTitle.Text = "⌨️ CONTROLLI"
    
    local controlsLabel = Instance.new("TextLabel")
    controlsLabel.Name = "Controls"
    controlsLabel.Size = UDim2.new(1, -30, 0, 80)
    controlsLabel.Position = UDim2.new(0, 15, 0, 35)
    controlsLabel.BackgroundTransparency = 1
    controlsLabel.Font = Enum.Font.Gotham
    controlsLabel.TextSize = 11
    controlsLabel.TextColor3 = Color3.fromRGB(180, 180, 200)
    controlsLabel.TextWrapped = true
    controlsLabel.TextXAlignment = Enum.TextXAlignment.Left
    controlsLabel.TextYAlignment = Enum.TextYAlignment.Top
    controlsLabel.Parent = controlsSection
    controlsLabel.Text = "W/A/S/D: Movimento orizzontale\nMuovi Vista: Su/Giù automaticamente\nSpace: Hover (Smooth mode)\nCtrl: Scendi veloce / Accelera (Turbo)"
    
    -- ===== DRAG FUNCTIONALITY PREMIUM =====
    local dragging = false
    local dragStart
    local frameStart
    
    header.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            frameStart = mainFrame.Position
        end
    end)
    
    game:GetService("UserInputService").InputChanged:Connect(function(input, gameProcessed)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - dragStart
            mainFrame.Position = frameStart + UDim2.new(0, delta.X, 0, delta.Y)
        end
    end)
    
    game:GetService("UserInputService").InputEnded:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = false
        end
    end)
end

-- ================================
-- FUNZIONI DI VOLO
-- ================================
local function startFlying()
    if Config.flying then return end
    Config.flying = true
    
    print("🐉 VOLO ATTIVATO - Modalità: " .. Config.currentMode)
    
    if bodyVelocity then bodyVelocity:Destroy() end
    if bodyGyro then bodyGyro:Destroy() end
    
    bodyVelocity = Instance.new("BodyVelocity")
    bodyVelocity.Velocity = Vector3.new(0, 0, 0)
    bodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
    bodyVelocity.Parent = humanoidRootPart
    
    bodyGyro = Instance.new("BodyGyro")
    bodyGyro.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
    bodyGyro.P = 10000
    bodyGyro.D = 100
    bodyGyro.Parent = humanoidRootPart
    
    if connection then connection:Disconnect() end
    
    connection = game:GetService("RunService").RenderStepped:Connect(function()
        if not Config.flying or not bodyVelocity or not bodyGyro then return end
        
        local camera = workspace.CurrentCamera
        local moveDirection = Vector3.new(0, 0, 0)
        local inputService = game:GetService("UserInputService")
        
        -- Movimento orizzontale
        if inputService:IsKeyDown(Enum.KeyCode.W) then
            moveDirection = moveDirection + (camera.CFrame.LookVector * Vector3.new(1, 0, 1)).Unit
        end
        if inputService:IsKeyDown(Enum.KeyCode.S) then
            moveDirection = moveDirection - (camera.CFrame.LookVector * Vector3.new(1, 0, 1)).Unit
        end
        if inputService:IsKeyDown(Enum.KeyCode.A) then
            moveDirection = moveDirection - camera.CFrame.RightVector
        end
        if inputService:IsKeyDown(Enum.KeyCode.D) then
            moveDirection = moveDirection + camera.CFrame.RightVector
        end
        
        -- Movimento verticale basato sulla visuale
        moveDirection = moveDirection + (camera.CFrame.UpVector * camera.CFrame.LookVector.Y)
        
        -- Effetti modalità
        if Config.currentMode == "Smooth" then
            if inputService:IsKeyDown(Enum.KeyCode.Space) then
                moveDirection = moveDirection + Vector3.new(0, 1, 0)
            end
            if inputService:IsKeyDown(Enum.KeyCode.LeftControl) then
                moveDirection = moveDirection - Vector3.new(0, 1, 0)
            end
        elseif Config.currentMode == "Turbo" then
            if inputService:IsKeyDown(Enum.KeyCode.LeftControl) then
                Config.speed = clamp(Config.speed + Config.acceleration, 1, Config.maxSpeed)
            end
        end
        
        if moveDirection.Magnitude > 0 then
            bodyVelocity.Velocity = moveDirection.Unit * Config.speed
        else
            bodyVelocity.Velocity = Vector3.new(0, 0, 0)
        end
        
        bodyGyro.CFrame = camera.CFrame
        
        if speedLabel then
            speedLabel.Text = round(Config.speed, 1) .. " / " .. Config.maxSpeed
        end
    end)
end

local function stopFlying()
    if not Config.flying then return end
    Config.flying = false
    
    if connection then
        connection:Disconnect()
        connection = nil
    end
    
    if bodyVelocity then
        bodyVelocity:Destroy()
        bodyVelocity = nil
    end
    
    if bodyGyro then
        bodyGyro:Destroy()
        bodyGyro = nil
    end
    
    print("⛔ VOLO DISATTIVATO!")
end

-- ================================
-- EVENT HANDLERS
-- ================================
player.CharacterAdded:Connect(function()
    stopFlying()
    wait(0.5)
    character = player.Character or player.CharacterAdded:Wait()
    humanoidRootPart = character:WaitForChild("HumanoidRootPart")
    humanoid = character:WaitForChild("Humanoid")
    createGui()
end)

humanoid.Died:Connect(function()
    stopFlying()
end)

-- ================================
-- INIZIALIZZAZIONE
-- ================================
createGui()

print("✅ DRAGON FLY PRO V2 FIXED CARICATO!")
print("🐉 Pannello GUI aperto - PULSANTE RIPARATO!")
